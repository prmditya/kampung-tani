# Backend Environment Configuration Template
# Copy this file to .env.local and configure with your actual values
# DO NOT commit .env.local files to version control

# ===== JWT CONFIGURATION =====
# Use a strong, random secret key (minimum 32 characters)
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-32-chars-minimum
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_HOURS=24

# ===== API CONFIGURATION =====
# API prefix used by the FastAPI app (must match README/docs examples)
API_PREFIX=/api/v1
# Interactive API documentation URLs (relative to host)
DOCS_URL=/api/v1/docs
REDOC_URL=/api/v1/redoc

# ===== CORS CONFIGURATION =====
# Comma-separated list of allowed origins. This variable name must match
# the backend Settings field `ALLOWED_ORIGINS` used by the application.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
